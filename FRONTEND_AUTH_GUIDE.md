# Frontend Authentication Guide

> **Retired.**
>
> The frontend authentication is implemented and the code documents itself
> (see `lib/auth/` and `lib/axios/`). The canonical documentation is in the
> backend repo: [`../Focura-backend/AUTHENTICATION.md`](../Focura-backend/AUTHENTICATION.md)
