export interface ColorTokens {
  bg: string;
  text: string;
  border: string;
  pill: string;
}

export interface GuideSection {
  id: string;
  icon: string;
  label: string;
  color: string;
  title: string;
  subtitle: string;
}